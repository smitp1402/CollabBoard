# Firebase Web App Config (from Firebase Console > Project settings > Your apps)
# Copy to .env.local and fill in your values.

NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=

# Realtime Database URL (required for Phase 4 cursors & presence)
# From Firebase Console: Realtime Database → Create database → copy URL
NEXT_PUBLIC_FIREBASE_DATABASE_URL=

# Firebase Admin SDK (server-only; for API auth and Firestore in API routes)
# From Firebase Console: Project settings → Service accounts → Generate new private key
# Paste client_email and private_key from the downloaded JSON:
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=

# OpenAI (server-only; for AI tool-calling in app/api/ai/commands)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# AI Agent feature flag. Set to "false" or "0" to disable AI commands (returns 503).
AI_AGENT_ENABLED=true

# Rate limits: max AI commands per window (1 minute). Optional; defaults: 30/user, 60/board.
# AI_COMMANDS_PER_USER_PER_MINUTE=30
# AI_COMMANDS_PER_BOARD_PER_MINUTE=60
