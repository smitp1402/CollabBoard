/** Target frame rate during pan, zoom, object manipulation (FPS). */
export const PERF_TARGET_FPS = 60;

/** Target object sync latency in milliseconds. */
export const PERF_TARGET_OBJECT_SYNC_MS = 100;

/** Target cursor sync latency in milliseconds. */
export const PERF_TARGET_CURSOR_SYNC_MS = 50;

/** Target object capacity (minimum objects without performance drops). */
export const PERF_TARGET_OBJECT_COUNT = 500;

/** Target concurrent users without degradation. */
export const PERF_TARGET_CONCURRENT_USERS = 5;
